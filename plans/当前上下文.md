# Momo 当前压缩上下文

更新时间：2026-05-17

## 已完成

- 仓库在 `main` 直接开发，用户要求每阶段验证可运行后提交，后续提交信息使用中文。
- 桌面端位于 `apps/desktop`，技术栈为 Tauri 2 + Vue 3 + TypeScript + Vite。
- 已按架构要求将桌面端 UI 层从 React 迁移到 Vue 3：
  - Vite 插件切到 `@vitejs/plugin-vue`，构建脚本切到 `vue-tsc --noEmit && vite build`。
  - 入口改为 `src/main.ts` + `App.vue` + `vue-router`，页面组件改为 `.vue` SFC。
  - `TaskRepository`、sync runner、contracts 和 Tauri/Rust 边界保持不变。
  - 页面测试改为 `@testing-library/vue`，并新增 tooling 回归防止 React 依赖回流。
  - `plans/设计.md` 已写入“桌面共享 UI 层统一使用 Vue 3 + TypeScript + Vue Router”的架构要求。
  - 已验证通过：`npm run test`、`npm run build`、`npm run verify`。
- 已按历史提交记录完成 Vue 迁移功能核对：
  - `dba97ef` / `66b4767`：登录占位、主壳路由、Today / Inbox / Calendar / Settings 本地任务页面已由 `App.vue`、`router.ts` 与各 `.vue` 页面承接。
  - `7eb5e38`：`/widget` 紧凑路由与侧栏 `Open widget` 入口已由 `Widget.vue`、`AppShell.vue` 与 `widgetWindow.ts` 承接。
  - `d096048` / `c783e36` / `eeef56d`：任务字段编辑、dueAt / estimateMin、加载错误 Retry 与清空字段语义已在 Vue 页面测试覆盖。
  - `dbd3912` 至 `188ffde`：Pending sync、Sync conflicts、Sync status 等 Settings 可见性已在 `Settings.vue` 和页面测试覆盖。
  - `fa35a59` 至 `6aac706`：本地同步模拟、sync runner 默认 Settings 入口与运行结果兼容已由 `App.vue` provide、`Settings.vue` inject/props 和页面测试覆盖。
  - `d760f1d` 至 `12a3666`：delta pull 应用摘要 `Pull applied` 已由 `Settings.vue` 展示并有页面测试覆盖。
  - `fd2d37e` 至 `361dfb2`：HTTP-like route/status 错误、runner 错误写回与 Settings 刷新同步状态仍由共享 sync 层和页面测试覆盖。
  - `7b4b946` 至 `b1e9e6f`：真实 HTTP transport contract、远程同步配置读取与默认 `/settings` 只读展示已由 `remoteSyncConfig.ts`、`App.vue`、`Settings.vue` 与测试覆盖。
  - 当前 `apps/desktop/src` 已无 React import、React Router、JSX/TSX 源入口；tooling 测试新增源码级回归检查。
- 已接入 `@tauri-apps/plugin-sql` / `tauri-plugin-sql`，SQLite 连接固定为 `sqlite:momo.db`。
- 已实现 `TaskRepository`，页面不直接写 SQL。
- 已实现本地任务 MVP：
  - `Today`：快速添加今日任务，显示逾期、今日、今日完成。
  - `Inbox`：显示无截止日期任务，支持编辑、完成、删除。
  - `Calendar`：显示未来 7 天只读 agenda。
  - `Settings`：显示本地数据库状态。
- 已清理 Vite 模板遗留，README 已更新。
- 已实现桌面 Widget 壳：
  - `/widget` 紧凑路由。
  - 主窗口侧栏 `Open widget` 入口。
  - `widget` 窗口获得 SQL 与窗口权限。
  - 当前只做 Tauri WebviewWindow 创建/聚焦，不做 Win32 扩展样式桥接。
- 已补全基础任务字段体验：
  - Today 快速添加可选择 Today 或 Inbox。
  - Inbox 编辑支持 title、notes、priority。
- 已实现同步前本地操作日志：
  - SQLite 增加 `local_changes` 表。
  - `TaskRepository` 对 create/update/status/delete 追加本地变更记录。
  - 新增 `listPendingChanges()` 与 `markChangeSynced()`，为后续 Delta Sync 铺路。
- 已补全 dueAt / estimateMin 基础编辑：
  - Today 快速添加支持显式截止时间和估时分钟。
  - Inbox 编辑支持 dueAt 和 estimateMin。
- 已建立同步/API contract 骨架：
  - 新增 `packages/contracts` 共享 TypeScript 包。
  - 定义 Task DTO、LocalChange DTO、Delta Push/Pull 请求响应类型。
  - 新增 delta push/pull 请求构造函数，固定 `SYNC_CONTRACT_VERSION = 1`。
  - 桌面端测试覆盖 contract payload 形状，contract 包可独立 `tsc` 编译。
- 已完成本地 MVP 页面验收硬化：
  - Inbox / Calendar / Settings 的加载错误都提供可键盘操作的 Retry。
  - 错误状态不再和空状态同时显示。
  - Inbox 编辑支持把已有 dueAt / estimateMin 清空为 `null`。
  - Inbox 完成、删除、保存失败会留在页面并显示错误。
- 已完成最终桌面端 MVP 收束核查：
  - README 已同步当前 schema、`/widget` 路由、contract 包与验证命令。
  - 原始本地 MVP 范围已落地：SQLite、本地 Repository、Today / Inbox / Calendar / Settings、widget 壳、测试与文档。
  - 最终验证通过：`npm run test`、`npm run build`、`cargo check`、contract 包 `tsc`。
- 已固化持续开发循环原则：
  - 每阶段完成后必须更新本文件，压缩本阶段结果、验证命令、下一阶段目标和阶段循环原则。
  - 每阶段验证通过后使用中文提交信息提交，然后继续下一阶段。
- 已建立最小 API / Sync 服务骨架：
  - 新增 `apps/api` 纯 TypeScript 包。
  - 新增 `createSyncApi()` 与 `createInMemorySyncStore()`，支持 delta push / pull 的内存语义验证。
  - 当前只接共享 contract，不启动 HTTP server，不接 OIDC、PostgreSQL、Redis、WebSocket 或生产环境。
  - 桌面 Vitest 覆盖 push/pull 与不支持 contract version 的拒绝路径。
- 已补齐 Sync 服务本地语义覆盖：
  - 支持 `task.update`、`task.status`、`task.delete` 本地应用。
  - 支持基于 `cursor-N` 的增量 pull。
  - 同一 local change id 重放保持幂等，不重复推进 cursor。
  - 被拒绝的变更返回拒绝原因且不推进 server cursor。
- 已建立 Core API 任务 CRUD 服务骨架：
  - `apps/api/src/tasks.ts` 新增纯 TypeScript `TaskService` 与 `TaskRepository` 接口。
  - 新增 `createInMemoryTaskRepository()` 内存实现。
  - 支持 create/update/status/delete/list，复用 `TaskDto`。
  - 新增 `TaskActor` 权限占位参数，viewer 写入会被拒绝，workspace list 隔离有测试覆盖。
- 已建立 API 路由适配层骨架：
  - `apps/api/src/router.ts` 新增 HTTP-like `createApiRouter()`。
  - 支持 `/tasks`、`/sync/delta/push`、`/sync/delta/pull` 的纯函数请求分派。
  - 覆盖 JSON parse、状态码、错误响应、actor headers 注入占位。
  - 仍不启动真实 server，不接 OIDC / PostgreSQL / WebSocket。
- 已建立仓库级本地验证入口：
  - 根目录新增 `package.json`。
  - 新增 `npm run verify`，串行执行 desktop test/build、Tauri cargo check、contracts tsc、api tsc。
  - 新增测试确保验证脚本不会漂移。
- 已建立 Sync 冲突对象 contract 骨架：
  - `packages/contracts` 新增 `TaskConflictDto` 与 `createTaskConflict()`。
  - `DeltaPushResponse` 新增 `conflicts` 字段。
  - 当前 `apps/api` 内存 sync 在无冲突场景返回 `conflicts: []`。
- 已实现 Sync 最小冲突检测：
  - `apps/api` 内存 sync 支持 update/status payload 的 `baseVersion` 检测。
  - 客户端基于旧版本修改任务时返回 `TaskConflictDto`。
  - 冲突变更不进入 accepted/rejected，不应用变更，不推进 cursor。
- 已建立 Sync 冲突解决 contract/API 骨架：
  - `packages/contracts` 新增 `ResolveTaskConflictRequest/Response` 与 `createResolveTaskConflictRequest()`。
  - `apps/api` 的 `SyncApi` 新增 `resolveConflict()`。
  - 内存 sync 会保存检测到的 conflict，`server_wins` 可标记 resolved 并返回当前 server task。
  - `client_wins` / `manual` 目前作为 pending manual 占位，不做复杂 merge。
- 已把冲突解决接入 API 路由适配层：
  - `apps/api/src/router.ts` 支持 `POST /sync/conflicts/resolve`。
  - route 测试覆盖成功解决冲突和 missing conflict 的 404 错误响应。
- 已固化 API route manifest 与本地 runbook：
  - `apps/api/src/router.ts` 导出 `API_ROUTES`，测试覆盖全部 HTTP-like endpoint。
  - `apps/api/README.md` 记录当前 routes、actor header 占位、验证命令和明确限制。
- 已建立桌面端同步请求构造适配层：
  - `apps/desktop/src/sync/syncClient.ts` 新增 `buildDeltaPushFromPendingChanges()`。
  - 可从 `TaskRepository.listPendingChanges()` 构造 `DeltaPushRequest`。
  - 测试覆盖 LocalChange -> LocalChangeDto 映射、空队列、workspace/device 参数。
- 已建立桌面端同步响应处理适配层：
  - `apps/desktop/src/sync/syncClient.ts` 新增 `applyDeltaPushResponse()`。
  - accepted change ids 会调用 `TaskRepository.markChangeSynced()`。
  - rejected changes / conflicts 暂作为摘要返回给上层，不自动解决。
- 已在 Settings 暴露本地同步队列状态：
  - `DatabaseStats` 新增 `pendingLocalChanges`。
  - `TaskRepository.getStats()` 统计 `local_changes.synced_at IS NULL`。
  - Settings 的 Local database 卡片显示 `Pending sync`。
- 已补齐本地同步文档与验收说明：
  - `apps/desktop/README.md` 记录 `Pending sync`、同步请求/响应适配层和 `npm run verify`。
  - 新增 README 回归测试，防止本地同步说明漂移。
- 已实现 `client_wins` 冲突解决策略：
  - `apps/api` 内存 sync 可将冲突中的 client payload 应用到当前 server task。
  - `client_wins` 会推进 cursor，返回 resolved task，并标记原 conflict resolved。
- 已固化 `manual` 冲突解决占位语义：
  - `manual` 返回 `pending_manual` 与 `resolvedTask: null`。
  - 不应用 client change、不删除 conflict、不推进 `serverCursor`。
  - HTTP-like route 对 `pending_manual` 返回 `202`，让上层可见该冲突仍待人工处理。
  - API README 已记录 `manual`/`202`/`pending_manual` 行为，并有回归测试防漂移。
- 已补齐待处理冲突只读查询：
  - `packages/contracts` 新增 `ListTaskConflictsRequest/Response` 与 `createListTaskConflictsRequest()`。
  - `apps/api` 的 `SyncApi` / `SyncStore` 新增 `listConflicts()`。
  - HTTP-like route manifest 新增 `GET /sync/conflicts`，返回 pending conflicts。
  - `manual` 后冲突仍会出现在列表；`server_wins` / `client_wins` 后从列表移除。
  - API README 已记录冲突列表路由，并有回归测试防漂移。
- 已建立桌面同步冲突摘要适配层：
  - `apps/desktop/src/sync/syncClient.ts` 新增 `summarizePendingConflicts()`。
  - 可把 `TaskConflictDto` 映射为只读展示摘要：conflict/task/change id、reason、createdAt、server task title/version、client payload summary。
  - 只做摘要，不自动解决、覆盖、重试或写入本地数据。
  - 桌面 README 已记录该适配层，并有回归测试防漂移。
- 已在 Settings 暴露同步冲突摘要占位：
  - `Settings` 支持注入 `pendingConflicts` 只读摘要列表，默认空列表。
  - 有冲突时显示 `Sync conflicts`、数量、server task 标题/版本和 client payload 摘要。
  - 不提供解决按钮，不触发冲突解决或本地写入。
  - 桌面 README 已记录该占位，并有页面测试与 README 回归测试。
- 已建立同步运行摘要类型与状态文案：
  - `apps/desktop/src/sync/syncClient.ts` 新增 `SyncRunSummary` 与 `summarizeDeltaPushResponse()`。
  - delta push 结果可归纳为 `all-synced`、`has-rejections`、`has-conflicts`。
  - `applyDeltaPushResponse()` 保留原始 accepted/rejected/conflicts/cursor，同时返回 `summary`。
  - 该层只做状态归纳，不发网络请求、不自动重试或解决冲突。
- 已在 Settings 增加同步运行摘要只读占位：
  - `Settings` 支持注入 `syncSummary`。
  - 有摘要时显示 `Sync status`、状态、message、accepted/rejected/conflict 计数和 cursor。
  - 不触发同步、不自动重试、不提供冲突解决按钮。
  - 桌面 README 已记录该占位，并有页面测试与 README 回归测试。
- 已补齐同步运行摘要的空变更语义：
  - `summarizeDeltaPushResponse()` 在 accepted/rejected/conflicts 全为空时仍返回 `all-synced`。
  - 空响应 message 为 `Already synced`，避免显示 `0 local changes synced`。
  - rejected/conflict 优先级保持不变。
- 已为同步适配层补齐导出状态回归：
  - `apps/desktop/src/sync/syncClient.ts` 新增 `SYNC_RUN_STATUSES` 常量。
  - `SyncRunStatus` 由该常量推导，减少状态枚举漂移风险。
  - 测试覆盖稳定状态列表与 `applyDeltaPushResponse()` 返回 `summary` 形状。
- 已收束同步可见性文档与下一轮路线：
  - `apps/api/README.md` 新增 `Sync visibility scope`，概述 cursor、`baseVersion` 冲突检测、`server_wins`/`client_wins`/`manual` 和 `GET /sync/conflicts`。
  - `apps/desktop/README.md` 明确下一轮建议先定义 `sync runner boundary` 或 `local simulation entrypoint`。
  - 文档继续强调当前仍无真实 HTTP server、认证、PostgreSQL、WebSocket 或后台同步任务。
- 已建立本地同步模拟入口：
  - `apps/desktop/src/sync/syncClient.ts` 新增 `runLocalSyncSimulation()`。
  - 入口会读取 pending changes、调用注入的内存 sync API `deltaPush()`、应用响应标记 accepted，再读取 `listConflicts()` 并返回冲突摘要。
  - 该入口只用于本地/测试端到端演练，不接真实网络。
  - 桌面 README 已记录该入口，并有同步客户端测试覆盖。
- 已为本地同步模拟入口补齐冲突路径：
  - `runLocalSyncSimulation()` 返回 `pendingConflictCount`。
  - 测试覆盖内存 sync API 返回 conflict 时 summary 为 `has-conflicts`、pending conflict summary 可见、冲突 change 不会被 `markChangeSynced()`。
  - 成功路径和冲突路径均已端到端覆盖。
- 已将本地同步模拟入口接入 Settings 演示态：
  - `Settings` 支持可选注入 `onRunLocalSyncSimulation`。
  - 有注入回调时展示 keyboard-accessible 的 `Local sync simulation` 普通按钮。
  - 点击按钮只调用注入回调，不发真实网络请求，不自动同步。
  - 成功后展示返回的 `Sync status` 与 `Sync conflicts`；失败时在页面内展示错误；运行中禁用按钮并显示 loading。
  - 桌面 README 已记录该演示入口，并有页面测试与 README 回归测试防漂移。
- 已建立桌面端 sync runner boundary：
  - `createSyncRunner()` 接收注入的 repository、transport、workspace/device 与 clock。
  - `runOnce()` 复用现有本地同步模拟编排：pending changes -> delta push -> accepted 标记 -> pending conflicts 摘要。
  - transport 错误会被归一为 `{ ok: false, error, result: null }`，避免直接抛进页面层。
  - 当前 runner 仍不拥有真实 HTTP、后台任务或 cursor 持久化。
  - 桌面 README 已记录 runner boundary，并有同步客户端测试与 README 回归测试。
- 已建立本地 sync cursor state boundary：
  - SQLite schema 新增 `sync_state` 表和 migration version 3。
  - `TaskRepository.getSyncState()` / `saveSyncState()` 读写 server cursor、last synced、last error 与 updatedAt。
  - Settings 读取并只读展示 `Sync state`，用于排查 cursor、最近同步时间和最近错误。
  - 当前只建立状态边界，不自动触发同步，不接真实 HTTP。
  - 桌面 README 已记录 `sync_state`、Repository 方法和 Settings 展示，并有 Repository、页面、README 回归测试。
- 已将 sync runner 结果写回本地 `sync_state`：
  - `createSyncRunner()` 的 repository 边界扩展为包含 `saveSyncState()`。
  - `runOnce()` 成功时保存 server cursor / last synced，并清空 last error。
  - `runOnce()` 捕获 transport 错误时保存 last error，并继续返回 `{ ok: false, error, result: null }`。
  - Settings 的模拟回调兼容旧 `LocalSyncSimulationResult` 和新 `SyncRunnerRunOnceResult`，页面仍不直接理解 transport。
  - 桌面 README 已记录 runner 状态写回语义，并有同步客户端、页面、README 回归测试。
- 已给默认 Settings 路由接入本地 runner factory：
  - 新增 `createLocalSyncRunner()`，使用 in-memory transport 包装本地 `createSyncApi()`。
  - `App` 在 `/settings` 默认路由中创建 local runner，并把 `runOnce()` 注入给 `Settings`。
  - 实际桌面壳的 `/settings` 已能看到 `Local sync simulation` 演示按钮。
  - 该入口仍只用于本地演示，不接真实 HTTP，不持有账号，不自动后台同步。
  - 桌面 README 已记录 default Settings route / in-memory transport / local simulation entrypoint，并有页面与 README 回归测试。
- 已补齐本地同步演示手动验收与开发态检查：
  - README 新增 `Manual acceptance`，区分 Vite smoke 与 Tauri WebView 完整 SQLite 成功链路。
  - Vite smoke 打开 `http://127.0.0.1:1420/settings`，确认路由、`Local sync simulation` 按钮和错误捕获可见。
  - Vite 普通浏览器缺少 Tauri SQL `invoke`，因此只能验证 route/button/error handling；完整 SQLite 成功流需 `npm run tauri dev`。
  - 临时 Vite dev server 已停止，未保留 1420 监听进程。
- 已收束本地同步演示边界并规划下一步：
  - 桌面 README 新增 `Next sync boundary`。
  - 明确下一步优先做 `delta pull application boundary`，把服务端 delta pull 结果应用到本地 SQLite。
  - 该边界应复用 `sync_state.serverCursor` 作为 pull 起点，应用完成后写回新 cursor。
  - 真实 HTTP transport 仍排后，继续保持 in-memory transport / HTTP-like router 语义验证。
- 已实现 `delta pull application boundary`：
  - `TaskRepository.applyRemoteTask()` 可 upsert 远端任务到本地 `tasks`。
  - `TaskRepository.deleteRemoteTask()` 可删除远端已删除任务。
  - 远端 pull 应用不会写入 `local_changes`，避免把服务端变更重新当成本地待同步变更。
  - `applyDeltaPullResponse()` 会应用 `DeltaPullResponse.tasks/deletedTaskIds`，并用 `serverCursor` / `syncedAt` 更新 `sync_state`。
  - 桌面 README 已记录 pull 应用边界，并有 Repository、sync adapter、README 回归测试。
- 已将 sync runner 串上 delta pull 应用闭环：
  - `createSyncRunner().runOnce()` 在 delta push / pending conflicts 后，如果 transport 支持 `deltaPull()`，会读取 `sync_state.serverCursor` 作为 `sinceCursor`。
  - pull 返回的远端 task / deletion 会通过 `applyDeltaPullResponse()` 应用到本地 repository，并写回新的 server cursor。
  - runner 返回值包含可展示的 `pull` 摘要：applied task count、deleted task count、server cursor。
  - 当前仍只使用 in-memory / HTTP-like transport 边界，不接真实 HTTP，不做后台自动同步。
  - 已通过定向验证：`npm run test -- tests/desktopSyncClient.test.ts`、`npm run test -- tests/readme.test.ts`、`npm run build`。
- 已在 Settings 展示 pull 应用摘要：
  - 本地同步演示返回 runner result 且包含 `pull` 时，Settings 显示只读 `Pull applied` 卡片。
  - 卡片展示 applied tasks、deleted tasks 和 pull cursor，不新增 pull 按钮，不触发真实 HTTP 或后台同步。
  - 桌面 README 与手动验收说明已补充 `Pull applied`，并有页面测试与 README 回归测试。
  - 已通过定向验证：`npm run test -- tests/pages.test.ts`、`npm run test -- tests/readme.test.ts`。
- 已让 Settings 在同步演示成功后刷新本地状态：
  - `Local sync simulation` 成功返回后会重新调用 repository `getStats()` 与 `getSyncState()`。
  - `Pending sync` 与 `Sync state` 会反映 runner 写回后的 pending count、cursor、last synced 与 last error。
  - 失败路径仍只显示页面内错误，不会误刷新或覆盖现有同步状态。
  - 桌面 README 已记录该刷新语义，并有页面测试与 README 回归测试。
  - 已通过定向验证：`npm run test -- tests/pages.test.ts`、`npm run test -- tests/readme.test.ts`。
- 已建立 HTTP-like sync transport 适配边界：
  - 新增 `createHttpLikeSyncTransport()`，把 `deltaPush()` / `deltaPull()` / `listConflicts()` 映射到 `ApiRouter.handle()`。
  - route 映射覆盖 `/sync/delta/push`、`/sync/delta/pull` 与 `/sync/conflicts`，非 2xx 响应会提升为 Error 交给 runner 错误边界。
  - `createLocalSyncRunner()` 改为通过 in-memory API router + HTTP-like transport 调用本地 sync API，Settings 默认演示路径不再绕过 router。
  - 当前仍不发真实网络请求，不接账号、认证、后台任务或生产 HTTP server。
  - 已通过定向验证：`npm run test -- tests/desktopSyncClient.test.ts`、`npm run test -- tests/readme.test.ts`、`npm run build`。
- 已补齐 HTTP-like transport 错误可见性：
  - Settings 在 `SyncRunnerRunOnceResult.ok === false` 时也会重新读取 repository `getStats()` / `getSyncState()`。
  - 页面同时展示本次 simulation error 和 runner 写回到 `sync_state.lastError` 的本地状态。
  - README 已记录 sync errors refresh `Sync state`，页面测试与 README 回归测试覆盖该行为。
  - 已通过定向验证：`npm run test -- tests/pages.test.ts`、`npm run test -- tests/readme.test.ts`。
- 已细化 HTTP-like transport 错误信息：
  - `createHttpLikeSyncTransport()` 对非 2xx route 响应抛错时会包含 method、path、status 与 body error。
  - 例如 `POST /sync/delta/pull failed with 400: Unsupported sync contract version`。
  - README 已记录 route/status 错误格式，sync client 测试与 README 回归测试覆盖该行为。
  - 已通过定向验证：`npm run test -- tests/desktopSyncClient.test.ts`、`npm run test -- tests/readme.test.ts`。
- 已把 route/status 错误贯通到 runner：
  - `createSyncRunner()` 在 transport 失败时会保留原始 route/status 错误作为 `SyncRunnerRunOnceResult.error`。
  - `saveSyncState()` 写入失败时不会遮蔽原始 sync / transport 错误，避免页面只看到二次持久化错误。
  - README 已记录 `sync_state` 写入失败 does not hide the original sync error，sync client 测试与 README 回归测试覆盖该行为。
  - 已通过定向验证：`npm run test -- tests/desktopSyncClient.test.ts`、`npm run test -- tests/readme.test.ts`。
- 已定义真实 HTTP sync transport contract：
  - 新增 `createHttpSyncTransport()`，通过注入的 `fetch` 与 `baseUrl` 把 `SyncRunnerTransport` 请求发送为 JSON。
  - 当前覆盖 `/sync/delta/push`、`/sync/delta/pull`、`/sync/conflicts` 的请求 shape 与非 2xx 错误格式。
  - 该 transport 目前仅有 contract，不接默认 Settings 路径、不配置生产 URL、不自动发真实网络请求。
  - README 已记录 injected `fetch` 边界，sync client 测试与 README 回归测试覆盖该行为。
  - 已通过定向验证：`npm run test -- tests/desktopSyncClient.test.ts`、`npm run test -- tests/readme.test.ts`、`npm run build`。
- 已补齐 HTTP transport 配置边界：
  - `createHttpSyncTransport()` 支持可选 async/sync headers provider，用于注入 auth/client headers。
  - baseUrl 为空时会抛出 `HTTP sync baseUrl is not configured`，并且不会调用 fetch。
  - README 已记录 headers provider 与 baseUrl 配置错误，sync client 测试与 README 回归测试覆盖该行为。
  - 已通过定向验证：`npm run test -- tests/desktopSyncClient.test.ts`、`npm run test -- tests/readme.test.ts`、`npm run build`。
- 已定义远程同步配置读取边界：
  - 新增 `createRemoteSyncConfig()`，从配置对象读取 `VITE_MOMO_SYNC_BASE_URL` 与 `VITE_MOMO_SYNC_TOKEN`。
  - 未配置 base URL 时返回 disabled 状态，不创建远程 transport。
  - 配置 token 时生成 Bearer headers provider；无 token 时返回空 headers。
  - 当前仅定义配置解析，不把 Settings 默认 runner 切到真实 HTTP。
  - 已通过定向验证：`npm run test -- tests/desktopSyncClient.test.ts`、`npm run test -- tests/readme.test.ts`、`npm run build`。
- 已在 Settings 展示远程同步配置状态：
  - Settings 新增只读 `Remote sync config` 卡片。
  - disabled 时显示 reason；enabled 时显示 base URL 与 token configured 状态，不展示 token 原文。
  - 默认仍是 disabled，本地 Settings route 尚未读取 `import.meta.env`。
  - README 已记录该只读配置状态，页面测试与 README 回归测试覆盖该行为。
  - 已通过定向验证：`npm run test -- tests/pages.test.ts`、`npm run test -- tests/readme.test.ts`、`npm run build`。
- 已让默认 Settings 路由读取远程同步配置：
  - `App` 在 `/settings` 默认路由中调用 `createRemoteSyncConfig()`，读取 `import.meta.env` 中的 `VITE_MOMO_SYNC_BASE_URL` / `VITE_MOMO_SYNC_TOKEN`。
  - 该配置只用于 Settings 的 `Remote sync config` 只读展示。
  - 默认同步执行仍走本地 `createLocalSyncRunner()`，不接真实 HTTP transport，不发真实网络请求。
  - README 已记录 default `/settings` route reads env for display only，页面测试与 README 回归测试覆盖该行为。
  - 已通过定向验证：`npm run test -- tests/pages.test.ts`、`npm run test -- tests/readme.test.ts`、`npm run build`。
- 已明确远程配置 enabled 时仍使用本地演示：
  - Settings 的 `Remote sync config` 卡片新增只读 `Sync action`，固定显示 `Local simulation`。
  - enabled 远程配置仍只展示 base URL / token configured / action，不切换默认 runner，不发真实 HTTP。
  - README 已记录 `Sync action` / `Local simulation` 边界，页面测试与 README 回归测试覆盖该行为。
  - 已通过定向验证：`npm run test -- tests/pages.test.ts`、`npm run test -- tests/readme.test.ts`、`npm run build`。
- 已建立远程 sync runner factory 占位：
  - 新增 `createRemoteSyncRunner()`，把 `RemoteSyncConfig`、repository、workspace/device、clock 与 injected `fetch` 组装成可运行的 `SyncRunner`。
  - disabled config 返回 `{ kind: "disabled", reason, runner: null }`，不会触碰 fetch；enabled config 返回 `{ kind: "enabled", runner }`，只在显式调用时运行。
  - README 已记录 remote sync runner factory boundary，桌面同步客户端测试与 README 回归测试覆盖该行为。
  - 已通过定向验证：`npm run test -- tests/desktopSyncClient.test.ts`、`npm run test -- tests/readme.test.ts`、`npm run build`。
- 已锁定默认 Settings 路由仍只使用本地 runner：
  - 新增 `createDefaultSettingsSyncRuntime()`，集中管理默认 Settings 运行时边界。
  - 默认 `/settings` 继续只把 `RemoteSyncConfig` 用于只读展示，并把 `createLocalSyncRunner(repository).runOnce()` 注入为 `Local sync simulation`。
  - tooling 回归确认 `App.vue` 和默认 runtime 不直接 import / instantiate `createRemoteSyncRunner()`。
  - README 已记录 default settings sync runtime，默认 runtime 测试、tooling 测试、页面测试覆盖该行为。
  - 已通过定向验证：`npm run test -- tests/defaultSettingsSyncRuntime.test.ts`、`npm run test -- tests/tooling.test.ts`、`npm run test -- tests/pages.test.ts`。
- 已整理远程同步边界手动验收说明：
  - README 的 Manual acceptance 新增 Remote config display smoke。
  - 可用 `VITE_MOMO_SYNC_BASE_URL=https://api.example.test/momo` 与 `VITE_MOMO_SYNC_TOKEN=local-dev-token` 只验证 `Remote sync config` enabled 展示、token 不泄漏和 `Sync action` 仍为 `Local simulation`。
  - 文档明确 default button still uses local simulation，不会因 env 存在而改走真实 HTTP。
  - README 回归测试覆盖这些措辞，防止后续文档误写成远程默认启用。
  - 已通过定向验证：`npm run test -- tests/readme.test.ts`、`npm run build`。
- 已建立本地同步运行历史记录边界：
  - SQLite schema 新增 `sync_runs` 表和 migration version 4。
  - `TaskRepository.recordSyncRun()` / `listRecentSyncRuns(limit)` 可记录和读取手动同步运行历史。
  - `createSyncRunner().runOnce()` 成功/失败后 best-effort 写入 `sync_runs`，记录 status、startedAt、finishedAt、message 与 cursor；历史写入失败不遮蔽本次同步结果。
  - README 已记录 sync run history，Repository、sync runner、README 与页面测试覆盖该行为。
  - 已通过定向验证：`npm run test -- tests/tasks.test.ts`、`npm run test -- tests/desktopSyncClient.test.ts`、`npm run test -- tests/readme.test.ts`、`npm run test -- tests/pages.test.ts`、`npm run build`。
- 已在 Settings 展示最近同步运行历史：
  - Settings 读取 `TaskRepository.listRecentSyncRuns(3)`，展示只读 `Sync history` 卡片。
  - 历史项展示 status、message、cursor、startedAt 与 finishedAt，不新增同步按钮，不接真实 HTTP。
  - 本地同步演示成功或失败后会刷新 history，让本次手动运行结果可追溯。
  - `Sync history` 加载失败只影响局部卡片和 `Retry sync history`，不会遮蔽 Local database 或 Sync state。
  - README 已记录 `Sync history`、`listRecentSyncRuns(3)` 和局部错误边界，页面测试与 README 回归测试覆盖该行为。
  - 已通过定向验证：`npm run test -- tests/pages.test.ts`、`npm run test -- tests/readme.test.ts`、`npm run build`。
- 已在 Settings 展示本地待同步变更摘要：
  - `apps/desktop/src/sync/syncClient.ts` 新增 `summarizePendingLocalChanges()`，把 `LocalChange` 映射为只读摘要。
  - Settings 读取 `TaskRepository.listPendingChanges()`，展示 `Pending changes` 卡片：change id、entity label、action、createdAt 和 payload 摘要。
  - 本地同步演示成功或失败后会刷新 pending changes；空队列时卡片收起，不提供重放、删除或强制标记 synced 操作。
  - `Pending changes` 加载失败只影响局部卡片和 `Retry pending changes`，不会遮蔽 Local database、Sync state 或 Sync history。
  - README 已记录 pending changes 摘要、mapper 和局部错误边界，sync client、页面与 README 回归测试覆盖该行为。
  - 已通过定向验证：`npm run test -- tests/desktopSyncClient.test.ts`、`npm run test -- tests/pages.test.ts`、`npm run test -- tests/readme.test.ts`。
- 已建立本地任务远端版本基线：
  - SQLite schema 新增 `task_sync_versions` 表和 migration version 5。
  - `TaskRepository.applyRemoteTask(task, remoteVersion)` 在 delta pull 应用时保存远端 task version，不把 version 泄进通用 Task UI 模型。
  - `TaskRepository.updateTask()` / `setStatus()` 记录 `local_changes` 时，如存在远端版本，会写入 `baseVersion`，用于 API 侧真实冲突检测。
  - `TaskRepository.deleteRemoteTask()` 会清理对应 `task_sync_versions`，避免远端删除后留下陈旧版本基线。
  - `applyDeltaPullResponse()` 会把 `TaskDto.version` 传给 repository。
  - README 已记录 `task_sync_versions`、`applyRemoteTask(task, remoteVersion)` 与 `baseVersion` 语义，Repository / sync adapter / README 回归测试覆盖该行为。
  - 已通过定向验证：`npm run test -- tests/tasks.test.ts`、`npm run test -- tests/desktopSyncClient.test.ts`、`npm run test -- tests/readme.test.ts`。
- 已在 Settings 展示同步 rejected changes 只读明细：
  - Settings 从本地同步演示 runner result 的 `push.rejectedChanges` 派生 `Sync rejections` 卡片。
  - 卡片展示 rejected change id 与 reason，只读，不提供自动重试、删除、覆盖或强制标记 synced 操作。
  - README 已记录 `Sync rejections` 与 `Rejected changes are read-only` 限制，页面测试与 README 回归测试覆盖该行为。
  - 已通过定向验证：`npm run test -- tests/pages.test.ts`、`npm run test -- tests/readme.test.ts`。
- 已将同步 rejected changes 关联到本地待同步变更摘要：
  - `apps/desktop/src/sync/syncClient.ts` 新增 `summarizeRejectedChanges()` 与 `RejectedChangeSummary`，按 change id 把 rejected change 关联到 `PendingLocalChangeSummary`。
  - Settings 的 `Sync rejections` 在可匹配本地 pending change 时显示 action、entity label 与 payload summary；本地变更缺失时仍保留 id/reason。
  - 该视图保持只读，不自动重试、不删除 `local_changes`、不强制 mark synced、不接真实 HTTP。
  - README 已记录 `summarizeRejectedChanges()` 与 rejected 行可显示 matching pending change action/entity/payload summary。
  - 已通过完整验证：`npm run verify`；`git diff --check` 仅有 Windows LF/CRLF 提示，无 whitespace error。
- 已将同步冲突摘要关联到本地待同步变更摘要：
  - `apps/desktop/src/sync/syncClient.ts` 新增 `summarizePendingConflictDetails()` 与 `PendingConflictDetailSummary`，按 `changeId` 把 pending conflict 关联到 `PendingLocalChangeSummary`。
  - Settings 的 `Sync conflicts` 在可匹配本地 pending change 时显示 action、entity label 与 local change created 时间；本地变更缺失时仍保留原 conflict 摘要。
  - 该视图保持只读，不自动解决冲突、不触发 `server_wins` / `client_wins` / `manual`、不删除 `local_changes`、不强制 mark synced、不接真实 HTTP。
  - README 已记录 `summarizePendingConflictDetails()` 与 conflict 行可显示 matching pending change action/entity/createdAt/payload summary。
  - 已通过完整验证：`npm run verify`；`git diff --check` 仅有 Windows LF/CRLF 提示，无 whitespace error。
- 已固化本地同步演示手动验收清单：
  - 新增 `apps/desktop/docs/local-sync-acceptance.md`，把 `/settings` 本地同步演示验收拆成 Vite smoke、Tauri WebView full SQLite flow、Remote config display smoke 和 Regression guardrails。
  - README 的 `Manual acceptance` 入口链接该清单，避免手动验收要求只散落在 README 段落里。
  - 新增 `apps/desktop/tests/localSyncAcceptance.test.ts`，回归检查清单覆盖 `Local sync simulation`、`Sync status`、`Sync state`、`Pending changes`、`Sync history`、`Sync rejections`、`Sync conflicts`、`Pull applied`、远程配置只读展示和 no real network 边界。
  - 清单明确默认 Settings 仍不调用 `createRemoteSyncRunner()`、不启动后台同步、不接真实网络、不提供自动冲突解决或 rejected retry/delete/force/mark-synced 操作。
  - 已通过完整验证：`npm run verify`；`git diff --check` 仅有 Windows LF/CRLF 提示，无 whitespace error。
- 已收束 BE-03 本地同步边界文档与下一路线：
  - README 的旧 `Next sync boundary` 不再指向已完成的 delta pull / HTTP config，改为 `BE-03 local sync boundary` 与 `Next roadmap boundary`。
  - README 明确 local BE-03 boundary 已足够支撑 desktop-only validation：push/pull adapters、cursor state、run history、pending diagnostics、rejected/conflict visibility、remote config display 与 local acceptance guardrails 已覆盖。
  - README 明确生产同步仍依赖 `BE-01 / IF-01 production backend prerequisites`，并把下一路线指向 `BE-04 realtime events`，同时继续要求 default Settings route 留在 local simulation。
  - 本阶段只更新文档与 README 回归测试，不实现 WebSocket、不接 OIDC/PostgreSQL、不切默认 runner。
  - 已通过完整验证：`npm run verify`；`git diff --check` 仅有 Windows LF/CRLF 提示，无 whitespace error。
- 已建立 BE-04 实时事件 contract 骨架：
  - `packages/contracts` 新增 `SyncEventDto`、`SyncEventTypeDto`、`ListSyncEventsRequest/Response`、`createSyncEvent()` 与 `createListSyncEventsRequest()`。
  - `apps/api` 新增 `createSyncEventApi()` 与 `createInMemorySyncEventStore()`，支持发布内存事件并按 `afterSequence` / `limit` 补齐。
  - HTTP-like route manifest 新增 `GET /sync/events` / `sync.listEvents`，用于只读 catch-up，不打开 WebSocket。
  - API README 与 desktop README 已明确 realtime events 当前只是 contract and in-memory semantics，不接 Redis/event bus、不接生产后端、不改变默认 Settings local simulation。
  - 已按 TDD 先观察红测：contracts 缺 `createSyncEvent()`、API 缺 `createInMemorySyncEventStore()`、router 缺 `GET /sync/events`、README 缺 realtime events 边界说明。
  - 已通过完整验证：`npm run verify`；`git diff --check` 仅有 Windows LF/CRLF 提示，无 whitespace error。

## 当前验证命令

在 `apps/desktop`：

```bash
npm run test
npm run build
```

在 `apps/desktop/src-tauri`：

```bash
cargo check
```

如本阶段触碰 `packages/contracts`，还需在仓库根目录运行：

```bash
.\apps\desktop\node_modules\.bin\tsc.cmd -p packages\contracts\tsconfig.json
```

如本阶段触碰 `apps/api`，还需在仓库根目录运行：

```bash
.\apps\desktop\node_modules\.bin\tsc.cmd -p apps\api\tsconfig.json
```

阶段提交前必须跑完相关命令并确认 exit 0。

## 开发规范

- 每阶段修改前先确认 `git status --short --branch`。
- 每阶段先写失败测试，再实现；测试文件优先放在 `apps/desktop/tests`。
- 每阶段验证通过后立即提交；提交信息用中文。
- 每完成一阶段，提交前必须对照 `plans/开发规划.md` 选择并写入下一阶段目标。
- 持续按路线图推进：每完成一个阶段，必须更新 `plans/当前上下文.md`，把本阶段结果、验证命令、下一阶段目标和本条阶段循环原则压缩进去，再提交，然后继续下一阶段。
- 任何对话上下文压缩都必须保留上一条原则：阶段完成后对照开发路线图决定下一阶段、更新当前上下文、验证、中文提交、继续下一阶段。
- 不接后端时，业务数据只走 `TaskRepository`，不要在页面直接写 SQL。
- 保留 Rust `greet` smoke test；除 Tauri 插件/窗口能力外，不把业务逻辑搬到 Rust。
- 不重写历史，不回滚用户或已提交改动。

## 下一阶段目标

阶段 73：让内存 sync API 发布实时事件。

- 继续推进 BE-04，但仍不启动真实 WebSocket server、不接 Redis/event bus、不接生产后端。
- 在 `apps/api` 设计可选事件发布边界，让 delta push 接受任务变更时发布 `task.changed`，检测冲突时发布 `conflict.raised`，冲突解决或同步运行摘要可视情况发布 `sync.run.updated`。
- 事件发布应保持内存语义和可测试边界：现有 `createSyncApi()` 不能强制依赖事件 store；默认 Settings local simulation 不因事件 API 存在而改变。
- 测试优先覆盖：accepted change 产生事件、conflict 产生事件、rejected change 不误报 task changed、route `GET /sync/events` 能补齐这些由 sync API 产生的事件。
- 更新 API README/desktop README 说明 sync event publishing 仍是本地内存骨架，不代表生产实时推送可用。
- 继续不做真实 HTTP/WebSocket 请求、后台任务自动调度、复杂 merge UI、OIDC、PostgreSQL、Redis、WebSocket server、通知系统或 Agent。
- 阶段完成后必须更新本文件、压缩上下文、验证、中文提交，再继续下一阶段。
